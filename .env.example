# Copy this file to `.env` or `.env.local` and fill in your real keys.
# Use service/admin keys only on the server side. Never commit real secrets.

# Supabase - client-side anon key (safe for browser reads, use service key only on server)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_anon_public_key_here

# Supabase server-side service key (keep secret; use only on server)
SUPABASE_SERVICE_KEY=your_service_role_key_here

# Optional: byLLM / litellm keys if using byLLM or litellm integrations
BYLLM_API_KEY=your_byllm_api_key_here

# App environment
NODE_ENV=development
